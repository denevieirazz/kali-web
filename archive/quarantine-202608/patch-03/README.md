# Patch 03 (histórico)

Status: **arquivado / não executar**.

O instalador adicionava um customizador baseado em `MutationObserver` e manipulação direta do DOM. A referência foi retirada após build, regressão e comparação Playwright sem alteração. O `cloudos-neon.css`, também citado pelo instalador, continua ativo e não faz parte desta remoção.

Os instaladores permanecem apenas como evidência histórica; seus payloads não devem ser reinstalados.
