name: Disabled Temporary Source Revision Wiring

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "temporary workflow disabled before removal"
