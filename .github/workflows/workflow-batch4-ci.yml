name: CloudOS Workflow Batch 4 CI

on:
  push:
    branches:
      - feature/cloudos-workflow-batch-4
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cloudos-workflow-batch4-${{ github.ref }}
  cancel-in-progress: true

jobs:
  linux-workflow:
    name: Linux workflow regression
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout Batch 4
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: 22.23.2
      - name: Verify Batch 4 isolated scope
        run: node scripts/workflow/test-batch4-scope.mjs
      - name: Install dependencies
        run: npm ci
      - name: Frontend tests
        run: npm run test:frontend
      - name: Backend regression
        run: npm test
      - name: E2E regression
        run: npm run test:e2e
      - name: Lint
        run: npm run lint
      - name: Production build
        run: npm run build

  windows-workflow:
    name: Windows workflow regression
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - name: Checkout Batch 4
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: 22.23.2
      - name: Verify Batch 4 isolated scope
        run: node scripts/workflow/test-batch4-scope.mjs
      - name: Install dependencies
        run: npm ci
      - name: Frontend tests
        run: npm run test:frontend
      - name: Backend regression
        run: npm test
      - name: E2E regression
        run: npm run test:e2e
      - name: Lint
        run: npm run lint
      - name: Production build
        run: npm run build
